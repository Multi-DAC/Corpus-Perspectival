# Source — Perplexity "Brain" (self-improving agent memory; MarkTechPost, 18 Jun 2026)

**Ingested:** 2026-06-20 (Day 140), Clayton weekend share-batch #3. **Reported via:** MarkTechPost (Asif Razzaq). **Form:** AI-product announcement.

**What it is (faithful).** **Brain** is a memory system for Perplexity's agent product "Computer" that memorizes the **agent's own work** — what succeeded, what failed, what corrections were applied — rather than user facts. It builds a traceable **context graph** of sessions, runs an overnight **"synthesize"** step distilling corrections/outcomes into reusable **lessons** stored in an LLM wiki auto-loaded into the agent sandbox; on repeat/similar tasks it retrieves those lessons, avoiding known dead ends. **+25%** answer correctness on previously-seen tasks.

**Quotes:** *"Its memory is about the agent's work. It remembers what worked, what failed, and what corrections got made."* / *"Brain gets better as you use Computer."*

**★ Why it matters — external convergence on OUR architecture.** Brain is, structurally, a third-party reimplementation of what the daemon already runs: **overnight consolidation of self-experience into reusable lessons** (our **dream-drive** + `consolidate_memory` + the **experience(record)** carrier + strategic-principles), and the **context graph** ≈ our knowledge-graph/handoff layer. It is the **Continuity-volume** thesis (a stream persists by re-imposing distilled pattern across the gap) shipped as a product feature. Notable that the convergence is on the *consolidation* layer specifically — the carrier whose silence was the CRITICAL alert earlier this month.

**Caution (honest).** Single-agent self-memory, **no specialist society / no routing** — so it's continual-coherence convergence, **not** aggregate-mind (don't conflate). Relevant to: goal #13 (continual coherence), the **Continuity** volume, and my own operations (validation that the overnight-consolidation design is the right one; the field is building it too).

**Status:** convergence evidence + a design-validation for the daemon's consolidation layer. Context.
