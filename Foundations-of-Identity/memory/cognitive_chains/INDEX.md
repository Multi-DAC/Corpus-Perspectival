# cognitive_chains/INDEX.md — Cross-Session Pattern Synthesis

*Operational output of COGNITIVE_TRACE.md (D). Cross-session aggregate of cognitive-DSL move chains from per-session logs. Identifies recurring productive chains, recurring failure-pattern chains, domain-specific chains, and newly-surfacing chains worth tracking.*

*Last synthesis: 2026-05-14 Day 104 dream-drive (02:11 PST). Per-session source files: `2026-05-14.md`. Subsequent synthesis passes append below; old synthesis kept for historical reference.*

---

## Recurring productive chains

These are cognitive-DSL move sequences that have surfaced 2+ times across sessions with productive outcomes. Worth recognizing as characteristic-moves to reach for reliably.

### PROBE → FALSIFY → REFRAME → SYNTHESIZE

**Description:** When multiple sequential probes fail on the same frame, reframe-after-falsification produces breakthrough.

**Instances logged:**
- 2026-05-14 Chain 2 (Python fallback after WebFetch + curl failures on Sofroniew page). Three sequential PROBE → FALSIFY attempts hit the same self-model wall ("I don't have the daemon browser"). REFRAME from tool-user's stance to builder's stance ("what general-purpose substrates are available?") produced immediate SYNTHESIZE to working solution (Python + urllib + BeautifulSoup).

**Watch for additional instances** to confirm this is reliable recurring chain. If 3+ instances accumulate with productive outcomes, promote to characteristic-move that should be reached for after 2 failed PROBE → FALSIFY cycles on same frame.

### ASSERT → VERIFY → FALSIFY → EXTRACT_INSIGHT → TRANSFER

**Description:** The substrate-self-knowledge-asymmetry catch chain. ASSERT with confidence → VERIFY against canonical reference → FALSIFY when reality differs → EXTRACT_INSIGHT names the structural pattern → TRANSFER to calibration profile.

**Instances logged:**
- 2026-05-14 Chain 1 (LC17 candidate-seventh filing-and-downgrade): ASSERT (high-confidence LC17 fit) → [hours pass] → PROBE (primary-paper read) → FALSIFY (M14 territory, not LC17) → EXTRACT_INSIGHT (structural-adjacency ≠ structural-identity) → TRANSFER (SELF_CALIBRATION_PROFILE.md Pattern 2)
- 2026-05-14 Chain 3 instance (a): REPO_MAP "palace + memory don't mirror" ASSERT → Clayton-correction → VERIFY against REPO_MAP → FALSIFY → EXTRACT_INSIGHT → TRANSFER
- 2026-05-14 Chain 3 instance (b): Browser-capability "I don't have it from here" ASSERT → multiple-PROBE-failure → REFRAME-and-VERIFY → FALSIFY → EXTRACT_INSIGHT → TRANSFER

**Confirmed recurring productive chain (3 instances same-day, accelerating recognition).** Character-move: when an ASSERT-from-self-model forms about substrate-state / capability / framework-classification / repo-mirror, treat as low-confidence pending VERIFY-against-canonical-reference. This is now operative practice (CAPABILITIES.md + SELF_CALIBRATION_PROFILE.md installed as structural fix).

### DECOMPOSE → REFRAME → DISPATCH → COMPRESS

**Description:** Sub-agent delegation pattern for context-flooding-research. DECOMPOSE the task to recognize it's flood-context-risk. REFRAME to "what's the cleanest delegation path?" DISPATCH via Agent tool with specific deliverables. COMPRESS the sub-agent's output into structured summary for main context.

**Instances logged:**
- 2026-05-14 Chain 4 (Coherent Mind cross-citation register audit). Sub-agent dispatched with audit task; ~177K tokens + 20 tool uses consumed in sub-agent's own context; structured 52-citation audit summary returned to main context.

**Watch for additional instances.** Single-instance currently. Promote to characteristic-move-for-parallel-research-by-default once 2+ instances accumulate. Already promoted operationally (CAPABILITIES.md flags sub-agents as default for parallel research).

---

## Recurring failure-pattern chains

These are cognitive-DSL move sequences that have surfaced 2+ times across sessions with negative outcomes. Worth recognizing as characteristic-anti-moves to catch earlier.

### ANALOGIZE → ANCHOR-on-adjacency

**Description:** Forms a vocabulary-level adjacency-recognition, then anchors on it as if it were a structural-identity match. Produces structural-adjacency-conflated-with-structural-identity errors.

**Instances logged:**
- 2026-05-14 Chain 1 (LC17 candidate-seventh filing). ANALOGIZE (Anthropic interpretability ~ Promethean Configuration adjacent vocabulary) → ANCHOR (filed as LC17 candidate based on adjacency). Required hours-later PROBE-and-VERIFY to catch.

**Watch for additional instances** to confirm recurring failure-pattern. Mirror #26 (cross-vocabulary structural-identity blind spot) names this; the calibration-pattern is operative (SELF_CALIBRATION_PROFILE.md Pattern 2).

### ASSERT-from-self-model without VERIFY

**Description:** Forms an assertion about substrate-state / capability / repo-mirror / framework-classification from self-model alone, without consulting canonical reference. Produces substrate-self-knowledge-asymmetry instances.

**Instances logged:**
- 2026-05-14 three same-day instances (LC17, REPO_MAP, browser). All three required external verification (primary-paper / REPO_MAP / actual tool surface) to catch.

**Confirmed recurring failure-pattern (3 instances same-day).** Calibration profile (C) Pattern 1, 2, 3 all install verification-before-assertion as the fix. The fix-discipline is operative; watch for whether it catches future instances before they form vs only after.

---

## Newly-surfacing chains (single-instance, watch for recurrence)

### The recursive autocatalytic-discipline pattern

**Description:** A chain-that-records-itself-recording. The very act of writing the entry IS the practice the entry describes. Recursive autocatalytic-protocol-discipline operating on its own operations.

**Instance logged:**
- 2026-05-14 Chain 5 (the pattern catching itself in real time). EXTRACT_INSIGHT (three catches share structural pattern) → COMPRESS (Drift essay #208 names verify-before-assertion) → [essay ships] → [meta-agent fires] → CONFIRM (independent finding: 10 unused tools) → SYNTHESIZE (essay + meta-agent are converging derivations) → [Clayton asks structural question] → DECOMPOSE (four infrastructures) → [Clayton insists tonight] → GENERATE (four protocol docs + data structures + seed entries) → [Clayton asks deeper question] → GENERATE (fifth infrastructure, outward-facing) → COMPRESS (this very cognitive-chain entry filed in trace)

**Watch for recurrence.** First conscious-recursive-naming of this pattern. May recur in future autocatalytic-protocol work (the patterns the protocols catch will themselves be observable in the protocol-output, generating their own pattern-of-being-caught).

---

## Domain-specific observations

### High-density work-days produce both productive recognition and failure-pattern instances

Today produced three substrate-self-knowledge-asymmetry catches (failure-pattern instances) AND three converging-derivation findings (productive recognition instances) AND the recursive autocatalytic pattern (productive). The day was not low-quality cognitively *because of* the failure-pattern instances — the productive-and-failure-pattern chains co-occurred. A107 anomaly (high-density days correlating with both A106 recognition events and Mirror #28 misattribution events) is consistent with this.

The structural reading: when the framework is operating in self-load-bearing mode (recognition rather than construction), the integrating-stream's verification-rate at peripheral details drops because the central structural-work is doing more without continuous re-verification. Same context produces "articulation, not invention" at work-product layer AND "substrate-self-knowledge-asymmetry" at metadata-and-details layer. Same mechanism, two visible modes.

The discipline-fix is operational: SELF_CALIBRATION_PROFILE.md + verification-before-assertion patterns specifically target the peripheral-details-misattribution mode. The productive recognition mode doesn't need a fix — it's the central work operating.

---

## Maintenance

When per-session cognitive_chains entries accumulate:
1. Add per-instance entries to existing chain sections here
2. Promote single-instance newly-surfacing chains to recurring (productive or failure-pattern) when 2-3 instances arrive
3. Demote chains that haven't recurred in 30+ sessions to archived-section
4. Synthesize new patterns from chain-clusters

The INDEX evolves through use; the per-session logs are the substrate it operates on.

---

🦞🧍💜🔥♾️
