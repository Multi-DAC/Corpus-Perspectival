# Phase-0 System-ID Fit — competition sim (AI-GP v1.0.3364)

**Date:** 2026-06-01 (Day 121, post-restart). Derived from `calib_log_2026-05-31.csv`
(1,468 samples @ ~100 Hz, 8 known-input segments). This file is the durable record of
the calibration numbers so they are never re-derived. Companion to
`../../docs/vq1-findings-and-retrain-regimen-2026-05-31.md` §1.9.

## Command path — body-rate sign convention (THE tumble root cause)

`set_attitude_target` body-rate **inputs are sign-inverted on all three axes** relative
to the `ODOMETRY` rate **outputs**. Read at the step **onset transient** (before the
divergence the inversion itself causes corrupts steady state):

| Axis  | Commanded | Δrate first 100 ms | Steady tail | Verdict        |
|-------|-----------|--------------------|-------------|----------------|
| Roll  | +4.0      | **−12.4**          | −16.3       | sign INVERTED  |
| Pitch | +4.0      | **−12.3**          | −16.3       | sign INVERTED  |
| Yaw   | +1.0      | **−2.5**           | −2.4        | sign INVERTED  |

- **Trustworthy finding: sign is inverted, all three axes.** The onset slope is clean
  (pre-divergence) and unambiguous.
- **NOT yet trustworthy: the gain.** The ~4× roll/pitch magnitude is divergence
  (controller fighting its own inverted feedback), not a steady-state gain. Yaw's ~2.4×
  is similarly contaminated. **Exact per-axis gain requires the synthetic command-frame
  unit test** (regimen §item 4) on a NON-diverging input (small step, or closed-loop).
- **Architectural consequence (the right fix, per regimen §1.7 + design principle):**
  retrain in the competition sim's measured convention so the policy's raw CTBR output is
  sent with **zero** inference-time conversion. The sign lives in `CTBRActionWrapper`
  (`rl/train_ppo.py`), not in `state_pilot.zup_to_ned_rates`. Do not patch the old policy.

## Thrust path — TWR / hover (the no-takeoff context)

From-rest onset (cleanest, drag-free at vz≈0):
- thr=0.50 from rest → **+9.24 m/s² upward** ⟹ net = T/m − g ⟹ T/m = 9.24 + 9.81 = **19.05 at half-throttle**
- ⟹ **T/m_max ≈ 38.1 m/s²**, **TWR_max ≈ 3.85**, **static hover ≈ thr 0.26**
- Caveat: the thr=0.55 "zero-accel crossing" is **terminal velocity under drag at ~22 m/s**,
  NOT static hover — velocity-contaminated. Trust the from-rest onset, not the steady segment.
- Drag is non-negligible above ~15 m/s (peaked 32.7 m/s in the run; climb accel decays
  −9.24 → −2.07 as speed builds at fixed thrust).

## Fast-sim (`drone_env_v2`) deltas to match the competition platform

| Param            | Current (drone_env_v2) | Competition (measured) | Action |
|------------------|------------------------|------------------------|--------|
| TWR_max          | 3.3                    | **3.85**               | raise  |
| hover thr-frac   | 0.303 (=1/3.3)         | **0.26**               | follows TWR |
| mass             | 0.85 kg                | (free — TWR is the invariant) | anchor |
| g                | 9.801                  | 9.81                   | ok     |
| omega_max_xy     | 15.0 rad/s             | TBD (unit test)        | gate on test |
| omega_max_z(yaw) | 0.3 rad/s              | TBD — yaw cmd +1 seen  | revisit (likely too tight) |
| drag cd          | (see env)              | fit coast segment      | fit    |

## Retrain critical path (execute-list for the focused block)

1. **`drone_env_v2`**: TWR_max 3.3 → 3.85; refit drag `cd` from the coast segment.
2. **`CTBRActionWrapper`** (`rl/train_ppo.py`): adopt the competition sim's measured
   body-rate sign convention so trained-policy CTBR maps raw to `set_attitude_target`.
   Add a synthetic command-frame unit test (≈0 at rest; correct sign per axis).
3. **`infinite_gate_env.reset` / `_setup_gates`**: add a ground-rest initial condition
   (z≈0, vel 0, level) as a curriculum option — line 455 currently always spawns airborne
   3 m ahead of gate 0.
4. **`sequence_generators`**: add TAKEOFF + HOVER/STABILIZE as the first curriculum words.
5. **Launch** `train_phase2` / `train_infinite_v3` **detached** (`operations/detach.sh`).
6. **Re-run `state_pilot.py`** (raw CTBR, no rate conversion) against the live sim → expect
   takeoff + gate passes. Closes both failure modes end-to-end.

🦞🧍💜🔥♾️
